# SplitPayment — Project todo's

- [ ] Spread around the code TODO items
- [ ] Remove unused CSS variables and stylign
- [ ] Simplify tailwind used around the components
  - [ ] Use globally defined variables for colors, fonts and paddings/margins (for paddings and marings framework defaults should be enough)
- [ ] Move from "use client" to SSR approach
- [ ] Add docker file that builds next app
- [ ] Add locale, i18n and translations that come with that
- [ ] Remove unreasenable defaults like hardcoded exchange rates for different currencies. Use only rates fetched from NBP and leave PLN as only default hardcoded option. No issues with exchange rates if theres no other currencies
- [ ] Refresh sum ("suma") value of calculation on every item value change instead on button press
- [ ] Remove all unused components
- [ ] Add E2E playwright tests. Create all basic flow test cases as E2E tests.
- [ ] Create 404, not found page, reuse cmon, do something and link to / main page
- [ ] Add cursor-pointer to all buttons
- [ ] Add functionality to add more "homies", the same way as we can add multiple items
- [ ] Make the application responsive, mobile UI should also be friendly
- [ ] Add settings with default list of "homies". Default for this list should be "me", "you"
